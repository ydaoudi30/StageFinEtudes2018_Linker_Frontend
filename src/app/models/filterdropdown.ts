export class FilterDropDown {
    label: string;
    textOnlyList: string[];

    constructor(label: string , textOnlyList: string[]) {
        this.label = label;
        this.textOnlyList = textOnlyList;
      }
}