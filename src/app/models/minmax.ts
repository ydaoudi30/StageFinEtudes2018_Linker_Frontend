export class MinMax {
    unit: string;
    min: string;
    max: string;

    constructor(unit: string , min: string, max: string) {
        this.unit = unit;
        this.min = min;
        this.max = max;
      }
}